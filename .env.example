# Optional. Defaults to /api. `.env.production` forces /api for `vite build` so localhost is never baked into the VPS bundle.
# VITE_API_URL=https://other-host.example.com/api
#
# Backend: set CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com on the API server if needed.
