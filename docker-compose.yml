services:
  project_frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventory_frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    networks:
      - project-network

networks:
  project-network:
    external: true
